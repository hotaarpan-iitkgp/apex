# Public Assets Directory

Place static image files (e.g. `.jpg`, `.png`, `.svg`, `.webp`) in this folder.
They will be accessible in the web app via root paths:

- Example: `/public/my-photo.jpg` -> reference as `<img src="/my-photo.jpg" />` in code.
